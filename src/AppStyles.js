const light = {
  mainThemeBackgroundColor: '#fff',
  textColor: '#382A2C',
  gray: 'rgba(59,35,39,0.30)',
};

const dark = {
  mainThemeBackgroundColor: '#1c1e24',
  textColor: '#fff',
  gray: 'rgba(229,224,224,0.7)',
};

const colorSet = { light, dark };

export default {
  colorSet,
};
